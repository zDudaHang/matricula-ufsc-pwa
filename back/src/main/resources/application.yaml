spring:
  application:
    name: matricula-ufsc-app
  # ===============================
  # = BANCO DE DADOS
  # ===============================
  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        jdbc:
          batch_size: 100
        order_inserts: true

server:
  port: 8080

# ===============================
# = APLICAÇÃO
# ===============================
jwt:
  secret: 80796d2a-b445-4c6d-b213-8afae94d88c5
app:
  firebase-configuration-file: firebase.json
